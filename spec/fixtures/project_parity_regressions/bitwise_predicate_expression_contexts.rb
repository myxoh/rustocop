formats << flag if (mask & flag) != 0
assert((minimum_job_args & job.keys) == minimum_job_args)
valid = (fmod - 4 & 0xffff).zero?

# Parentheses around the bit operation are required by RuboCop.
unchanged = mask & flag != 0
